export const smallSelect = {
    container: (provided) => ({
        ...provided,
        width: '187px'
     })
}
export const bigSelect = {
    container: (provided) => ({
        ...provided,
        width: '383px'
     })
}